package com.ediweb.education.client.handlers;

import java.util.logging.Logger;

public class ConnectCommandHandler implements Runnable {

    private static final Logger log = Logger.getLogger(ConnectCommandHandler.class.getName());

    @Override
    public void run() {

    }
}
